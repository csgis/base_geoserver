# =============================================================================
# CSGIS GEOSERVER 2.28 - GeoServer Configuration
# =============================================================================

# Sets the proxyBaseUrl in geoserver data config.
# Must match with .env SITE_URL but needs to include the SCHEME (https://)
PROXY_BASE=https://geoserver.example.com

# Needed for geoserver to run on root path (/)
GEOSERVER_PATH=/

# -----------------------------------------------------------------------------
# CORS Configuration
# -----------------------------------------------------------------------------
# Disable CORS in GeoServer since Traefik handles it
# Set to true if you need GeoServer to handle CORS directly
GEOSERVER_CORS_ENABLED=false

# If CORS is enabled, you can customize these:
# GEOSERVER_CORS_ALLOWED_ORIGINS=*
# GEOSERVER_CORS_ALLOWED_METHODS=GET,POST,PUT,DELETE,HEAD,OPTIONS
# GEOSERVER_CORS_ALLOWED_HEADERS=*
# GEOSERVER_CORS_URL_PATTERN=/*

# -----------------------------------------------------------------------------
# CSRF Protection
# -----------------------------------------------------------------------------
# Disable the CSRF check in proxy context
# If this is too insecure for your context, create a whitelist in web.xml
# https://docs.geoserver.org/stable/en/user/security/webadmin/csrf.html
GEOSERVER_CSRF_DISABLED=true

# -----------------------------------------------------------------------------
# Admin Password Configuration
# -----------------------------------------------------------------------------
# If set to true, configure.sh will update the admin password on every start
SET_PASSWORD_ON_UP=true
GEOSERVER_ADMIN_PASSWORD=geoserver_secure_password_change_me

# -----------------------------------------------------------------------------
# Sample Data
# -----------------------------------------------------------------------------
# If set to true, sample data will be added to geoserver (first run only)
SAMPLE_DATA=false

# -----------------------------------------------------------------------------
# JVM Configuration
# -----------------------------------------------------------------------------
# Configure the JVM startup options
# Adjust -Xms (min heap) and -Xmx (max heap) based on your server resources
CATALINA_OPTS=-server -Djava.awt.headless=true -Xms2G -Xmx5G -XX:NewSize=48m -DGEOSERVER_DATA_DIR=/var/local/geoserver

# -----------------------------------------------------------------------------
# Custom UID/GID (optional)
# -----------------------------------------------------------------------------
# Uncomment to match host user for mounted volume permissions
# CUSTOM_UID=1000
# CUSTOM_GID=1000
